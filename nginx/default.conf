# HTTP → HTTPS redirect
server {
    listen 80;
    server_name _;
    return 301 https://$host$request_uri;
}

# HTTPS
server {
    listen 443 ssl;
    server_name _;

    ssl_certificate     /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 10m;

    # 上傳檔案大小上限（配合 export-video route 的 150MB 設計）
    client_max_body_size 200M;

    # LLM streaming / SSE：關閉 buffering，避免回應被 nginx 暫存
    proxy_buffering off;
    proxy_cache     off;

    # 逾時配合 Cloud Run timeout（600s）
    proxy_read_timeout    600s;
    proxy_connect_timeout 10s;
    proxy_send_timeout    600s;

    # 大型 binary response（MP4 / WAV）：啟用 buffering 避免傳輸中途截斷
    location ~ ^/api/(export-video|generate-podcast|generate-music|normalize-podcast-audio) {
        proxy_pass            http://deckcast:3000;
        proxy_http_version    1.1;

        proxy_buffering          on;
        proxy_buffer_size        256k;
        proxy_buffers            32 1m;
        proxy_busy_buffers_size  32m;
        proxy_max_temp_file_size 0;

        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass         http://deckcast:3000;
        proxy_http_version 1.1;

        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }
}
